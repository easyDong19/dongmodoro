# PRD: week-plan

> 출처: docs/origin/pomodoro-prd.md §3.3 (초안) + [결정 원장](../../decision-log/2026-08-04-planning-session.md) §C (Q7·Q10~Q14), §A (Q8-1·Q9·plan_lead_days) + [리뷰 결정](../../decision-log/2026-08-04-review-decisions.md) D1·D3
> 초안과 이 문서가 다르면 이 문서가 이긴다. 특히 초안의 "일요일 시작 주 / ISO week 문자열 주 키"는 [ADR-010](../../architecture/decisions/adr-010-week-definition.md) 으로 폐기됐다.
> 원장 Q10 의 "pull 은 오늘이 속한 주로 제한"은 [ADR-012](../../architecture/decisions/adr-012-aggregation-predicate.md) 로 **폐기**됐고, `weeks.budget` NULL 파생은 [ADR-013](../../architecture/decisions/adr-013-baseline-budget-effect.md) 으로 **폐기**됐다.

## 문제

월간 마일스톤은 "이번 달이 끝나면 뭐가 달라져 있나"라는 상태 기술이고, 오늘 목록은
"지금 뭐 하나"라는 실행이다. 그 사이에 **숫자로 답하는 층**이 없으면 두 가지가 무너진다.

1. 마일스톤이 실행 가능한 크기로 쪼개지지 않아, 오늘 목록에 매일 새 계획을 즉흥으로
   짜게 된다 (원칙 2 위반 — 일 레이어는 pull 만 해야 한다).
2. 소진량을 비교할 기준(예산·est)이 없어, 세션 기록이 "얼마나 했다"는 사실만 남기고
   "계획 대비 어땠다"는 정보를 주지 못한다.

주 레이어는 이 두 구멍을 하나의 카드로 메운다. 동시에, 이 카드는 **계획이 0개여도
타이머가 완전히 동작해야 한다는 원칙 1을 깨지 않아야** 한다 — 있으면 풍부해지는
레이어여야 하고, 없으면 막히는 것이 없어야 한다.

## 대상 사용자

- 앱 소유자 1인 (로컬 단독, 로그인·멀티유저 없음)

## 목표

1. 주간 항목 하나가 "이번 주 이만큼"을 est 뽀모 수로 표현하고, 그 소진이 세션에서
   자동으로 파생돼 별도 입력 없이 갱신된다.
2. 주간 게이지와 항목별 합이 **정의상** 일치한다 — 집계 술어를
   `sessions.local_week = week_items.week` 로 고정하고, 화면의 기타 행을 차액으로
   정의해 이중 계상과 누락을 산술적으로 불가능하게 만든다 (ADR-012 §1·§4).
3. 계획 시점의 과적을 알리되 확정을 막지 않는다 — 예산은 추정치이고 초과는 실패가
   아니다 (원칙 3).
4. "시간을 다 쓴 것"과 "일이 끝난 것"을 구분한다 — 소진 ≥ est 는 완료가 아니다 (Q14).
5. 계획을 세우려는 사용자가 **어느 주를 고치는지 언제나 알 수 있다** — 편집 대상 주는
   달력에서 파생된 숨은 값이 아니라 화면에 보이는 선택 상태다.

## 범위 / 비범위

**범위:** 주 정의(월요일 시작·주 키)의 적용, 주간 카드 일반 뷰의 항목 목록, 주간
플래너(편집 대상 주 선택·항목 추가·est·마일스톤 연결·요일 배치·과적 경고·확정),
항목 드로어(자식 task 목록·생성·오늘로 가져오기), 항목 완료 제안·확정·폐기, 주차별
시스템 "기타" 항목, 주간 뽀모 예산 게이지, 이월 배지 표시.

**비범위:**

- 오늘 목록 자체의 관리(표시·체크·× 제거·자정 반납) → today-tasks. 이 문서는 "주간
  항목에서 오늘로 pull 한다"까지만 정의한다.
- 이월 3택(다음 주로 / 줄여서 / 보내주기)과 정산 판정·병합 → weekly-review. 이 문서는
  이월된 항목을 **표시**하는 규칙(배지)만 정의하고, 이월의 데이터 동작(재부모화·승계)은
  [ADR-012](../../architecture/decisions/adr-012-aggregation-predicate.md) §3 을 전제로 인용한다.
- `weekly_capacity` 정의 UI, 뽀모 길이(focus/short/long) 변경, 베이스라인 스냅샷의
  생성·관리, `weeks` 행 생성 시점과 예산 기본값 해석 → pomo-baseline. 이 문서는 그
  값을 예산 프리필과 요일 부하 기준선으로 **소비**하는 쪽이다.
- 마일스톤 자체의 생성·편집·월 이동 → milestones. 이 문서는 마일스톤 칩으로 연결하는
  것까지만.
- 3컬럼/탭 레이아웃, 카드가 어느 컬럼에 앉는지, 창 크기 하한 → app-shell.
- 스키마 DDL·쿼리·마이그레이션·제약 형태 → [ADR-011](../../architecture/decisions/adr-011-schema-final.md).
- task 단위 요일 배정 (요일 배치는 항목 단위뿐 — ADR-011 §8 에서 명시적 보류).
- 월 누적 소진 롤업 (v1.1 — Q15).
- **주 네비게이션.** v1 의 주간 카드에는 "지난 주 보기"·주 이동 화살표가 없다. 일반
  뷰는 항상 오늘이 속한 주이고, 편집 대상 주 선택(R3)은 이번 주 / 다음 주 두 값만
  갖는다. 과거 주를 되짚는 통로는 캘린더·주간 정산다.
- **항목 우선순위 정렬.** 사용자가 항목 순서를 직접 정하는 기능은 v1 비목표다 (R10).
- 미래 날짜 pull("내일 몫 미리 담기") — v1 비범위 (ADR-012 §2).

## 용어

| 용어 | 뜻 |
|---|---|
| **시스템 "기타" 항목** | `is_system = 1` 인 주차별 week_item. 부모 없는 task 가 붙는 실제 데이터 행 (R14) |
| **기타 행** | 주간 카드 목록 맨 아래에 렌더되는 **화면상의 한 행.** 시스템 항목 + 미분류 세션을 합친 표시 단위이고, 그 소진은 차액으로 정의된다 (R16) |
| **`마일스톤 없음`** | 플래너에서 "마일스톤에 연결하지 않음"을 고르는 칩의 라벨. 위 두 개념과 **무관하다** — 라벨을 `기타` 로 쓰지 않는 이유가 이것이다 |

## 요구사항

### 주의 정의와 편집 대상 주

- **R1.** 주는 **월요일에 시작**하고, 주 키는 그 주 월요일 날짜 `'YYYY-MM-DD'` 다
  (ADR-010 §1·§2). 요일을 다루는 모든 UI(요일 핍, 요일 칩, 요일 부하 그래프)는
  월요일부터 일요일 순으로 배열한다. `weekly_capacity` 배열 인덱스 0 = 월요일이다.
- **R2.** `W32` 같은 주 번호 라벨은 저장하지 않고 표시 시점에 계산한다 (렌더 전용).
  카드 헤더의 주 범위 표기도 주 키에서 파생한다.
- **R3.** 플래너의 **편집 대상 주는 파생값이 아니라 선택 가능한 상태**다. 값은
  `이번 주`(= 오늘이 속한 주)와 `다음 주`(= 그 다음 주) 둘이다.
  - 진입 시 기본 선택값은 `week_of(오늘 + plan_lead_days)` 이며 `plan_lead_days`
    기본값은 1이다 (ADR-010 §3) — 즉 일요일에는 `다음 주` 가, 평일에는 `이번 주` 가
    기본 선택된다.
  - 사용자는 플래너 안에서 이 선택을 언제든 바꿀 수 있다. **일요일에도 `이번 주` 를
    골라 진행 중인 주의 항목·est 를 고칠 수 있다** (R22 의 주중 재수정이 계획일에도
    무효가 되지 않는다).
  - 선택을 바꾸면 초안 목록·예산 입력·경고 계산이 그 주의 값으로 전부 다시 채워진다.
  - v1 에서 `plan_lead_days` 자체의 설정 UI 는 노출하지 않는다.
- **R4.** 일반 뷰가 보여주는 주는 **항상 오늘이 속한 주**다. 편집 대상 주와 다를 수
  있으며, 그때도 일반 뷰는 이번 주를 계속 보여준다.
- **R5.** 헤더 제목, 빈 상태 CTA 라벨, 플래너 확정 버튼 라벨은 **모두 R3 의 편집 대상
  주 선택 상태에서 파생한다.** 달력상의 오늘 요일에서 직접 파생하지 않는다 — 그렇게
  하면 "이번 주 할당 잡기"를 눌러 다음 주가 열리는 모순이 생긴다. 문구 표는
  [ux-spec](./ux-spec.md) §1·§5·§8 소관이다.

### 주간 항목

- **R6.** 주간 항목은 제목, est 뽀모 수, 마일스톤 연결(선택 — 미연결 시 `마일스톤 없음`),
  요일 배치 의도(선택 — 다중 선택, 미선택 = 미배치)를 갖는다.
  - est 는 **정수 ≥ 1** 이다. 단 **`is_system = 1` 인 시스템 "기타" 항목은 예외로
    est = 0** 이다 (R15) — 사용자가 만들 수 없고 플래너 입력을 거치지 않는 행이므로
    하한 1 을 적용하지 않는다. 이 예외를 컬럼 제약으로 어떻게 표현하는지는
    [ADR-011](../../architecture/decisions/adr-011-schema-final.md) §6 소관이다.
  - 마일스톤 미연결은 정상 상태다 — 연결은 선택이며, 마일스톤이 물리 삭제되면 연결이
    자동으로 끊겨 미연결이 된다 (ADR-014 §3).
- **R7.** 요일 배치는 **예약이 아니라 배치 의도**다. 배정된 요일이 되어도 항목이
  오늘 목록에 자동으로 들어가지 않는다. 오늘 배정된 항목은 목록 상단으로 정렬하고
  시각적으로 강조하는 것까지만 한다.
- **R8.** 항목의 소진 뽀모는 저장하지 않고 항상 세션에서 파생한다 (원칙 8). 집계 술어는
  **주 조건을 포함한다** (ADR-012 §1):

  ```
  항목 소진 = count(sessions s JOIN tasks t ON s.task_id = t.id
                    WHERE t.week_item_id = <항목>
                      AND s.kind = 'focus'
                      AND s.local_week = <그 항목의 week>)
  ```

  `sessions.local_week` 는 기록 시 1회 계산 후 불변이므로(ADR-009 §2, ADR-011 §3)
  **모든 focus 세션은 자기 주에서 정확히 한 번 세어진다.** 항목의 주와 어긋나는
  세션(주 경계를 넘긴 집중 등)은 그 주의 미분류로 떨어진다.
  이 술어를 화면이 각자 조립하지 않고 **한 곳(도메인 함수)에서만** 계산한다 —
  조건을 빠뜨리면 조용히 틀린 숫자가 나온다 (ADR-012 Consequences, ADR-008 계층 규칙).
- **R9.** 항목의 est 와 자식 task 들의 est 합은 **독립적인 값**이다 (Q13). 항목 est 는
  "이번 주 이만큼", task est 는 "이 조각 이만큼"이라는 서로 다른 질문에 답한다.
  두 값이 어긋나도 오류가 아니다.
  - 주간 정산의 **"남은 몫" 계산 기준은 항목 est** 이고, 값은 `max(0, est − 소진)` 으로
    **0 에서 클램프**한다 — 소진이 est 를 넘긴 항목의 남은 몫은 음수가 아니라 0 이다.
  - 이월을 "줄여서"로 처리할 때 **새 항목의 est 를 무엇으로 둘지는 weekly-review 가
    결정한다.** 이 문서는 그 결과 값을 표시하는 쪽이며 계산 주체가 아니다.

  > ⚠️ 가정: 드로어에서 "항목 est vs task est 합"의 어긋남을 사실로 표시할지는 **미결**
  > 이다 (Q13-B). 표시하면 정보가 되지만, 판단하지 않는다는 원칙 6 을 지키기 어려워질
  > 수 있다. v1 구현 시 재판단하며, 그때까지 이 문서는 어느 쪽도 요구하지 않는다.

- **R10.** 일반 뷰 항목 목록의 정렬은 **생성순(오래된 것부터)** 이고, 유일한 예외는
  "오늘 배정된 항목을 상단으로"(R7)다. 사용자가 순서를 직접 정하는 **항목 우선순위는
  v1 비목표**다 — 정렬 기준을 저장하는 컬럼을 만들지 않는다.
- **R11.** 이월된 항목에는 "N주째" 배지를 표시한다. N = `(week − origin_week)/7 + 1` 로
  계산하며(ADR-011 §4), `carry_from_id` 사슬 길이는 배지 값으로 쓰지 않는다 —
  건너뛴 주에서 값이 틀어진다. N = 1 일 때는 배지를 표시하지 않는다.
  - `origin_week` 는 항목이 **최초 생성된 주**를 박제한 값이며, **이월로 만들어진 새
    항목은 원본의 `origin_week` 를 승계한다(새 주로 재계산하지 않는다).** 승계하지
    않으면 이월 항목의 `origin_week` 가 매주 자신의 주와 같아져 배지가 영원히 1 이 되고,
    "3주째" 가 표시될 수 없다.
  - 이월은 `milestone_id` 도 함께 승계한다 (ADR-012 §3) — 승계하지 않으면 여러 주에
    걸친 결과물의 월간 진척이 주 경계마다 0 으로 초기화된다.

### 자식 task 와 pull

- **R12.** 항목 드로어에서 자식 task 목록을 보고, 새 task 를 제목 + est 로 생성하고,
  기존 task 를 다중 선택해 오늘 목록으로 가져올 수 있다. **첫 pull 은 생성이고 이후
  pull 은 선택**이다 — 자식 task 가 0개인 항목의 드로어는 목록 대신 새 task 입력만
  보이며, 그 항목을 처음 오늘로 가져오는 행위가 곧 첫 조각을 만드는 행위다.
  task 목록은 계획서가 아니라 그 항목으로 무엇을 해왔는지의 누적 기록이다.
- **R13.** **pull 대상에 주 제한이 없다** (ADR-012 §2). 다음 주 항목의 task 도 오늘
  목록으로 가져올 수 있고, 지난 주 항목의 미완료 조각도 재개할 수 있다. 드로어의
  `오늘로 가져오기` 버튼은 그 항목이 어느 주에 속하든 활성이다 — 비활성 사유는 항목이
  완료 상태인 경우뿐이다 (R26).
  - 다만 `task_pulls.pull_date` 에는 **오늘 날짜만** 기록한다. 미래 날짜 pull("내일 몫
    미리 담기")은 별개 결정이며 v1 비범위다.
  - 이 제한 해제가 집계를 깨지 않는 근거는 R8 의 주 조건이다 — 세션은 자기
    `local_week` 에서만 세어지므로, 다른 주 항목으로 한 집중은 그 주의 미분류로
    떨어지고 등식은 유지된다.
- **R14.** pull 은 `task_pulls(task_id, pull_date)` 행으로 기록되며, 오늘 목록에서
  빼도 그 날짜의 pull 이력은 남는다 (ADR-011 §2). 오늘 목록에서 빼는 것은 삭제가 아니다.

### 시스템 "기타" 항목과 미분류

- **R15.** 부모 주간 항목이 없는 task(오늘 목록에 직접 추가한 것, 사후 캡처로 소급
  생성된 것)는 시스템 "기타" 항목(`is_system = 1`)에 귀속된다 (Q7).
  - 어느 주의 기타 항목에 붙는지는 **그 세션의 `local_week` 기준**이다 — "오늘"이 속한
    주가 아니다 (ADR-012 §3). 자정을 걸친 세션에 사후 캡처로 조각을 붙일 때 두 값이
    다르다.
  - 이 항목은 실제로 필요해질 때만 생성한다(lazy) — 사용하지 않는 주에는 존재하지 않는다.
- **R16.** 기타 항목의 규칙: est 는 0 이며(R6 의 예외) **과적 경고와 요일별 부하 계산에
  산입하지 않는다**. 플래너 초안 목록에 나타나지 않으며 편집·삭제할 수 없다. 주간
  정산의 이월 3택 대상에서 제외된다.
- **R17.** 주간 카드에서는 기타 항목과 미분류 집중(task 에 연결되지 않은 focus 세션)을
  **한 행으로 통합**해 표시한다 (Q11). 행 이름은 "기타 — 계획에 없던 집중" 이다.
  - **그 행의 소진은 독립 계산하지 않고 차액으로 정의한다**
    (ADR-012 §4, 정의역은 [ADR-027](../../architecture/decisions/adr-027-other-row-domain.md) §1 로 정정):

    ```
    기타 행 소진 = 주간 총 소진 − Σ(목록에 표시되는 항목의 소진)
    ```

    `Σ` 의 정의역은 **그 주에 화면 목록으로 표시되는 항목** —
    `is_system = 0 AND dropped_at IS NULL AND deleted_at IS NULL` — 이고, 각 항목의
    소진은 R8 의 술어로 계산한다. 이 정의로 ① 시스템 항목이 `Σ` 에 다시 더해지는 이중
    계상과 ② 목록에 표시되지 않는 폐기 항목(R18)의 소진 누락이 동시에 닫힌다.
    **폐기 항목을 `Σ` 에 포함하면 A24 가 깨진다** — 그 소진이 차액에서 상쇄되어 기타
    행에 나타나지 않는다.
  - **주간 총 소진은 그 주 focus 세션 수 전체다.** 항목의 폐기·삭제가 이 값을 줄이지
    않는다 (ADR-027 §2). 같은 세션이 캘린더 점·타이머 기록에도 나타나므로, 항목을
    지웠다고 이 숫자가 줄면 세 화면이 서로 다른 사실을 말하게 된다.
  - **표시 조건 (세 갈래 중 하나라도 참이면 표시):**
    ① 그 주에 미분류 focus 세션이 있다 · ② 부모 없는 조각이 있다 ·
    ③ **기타 행 소진 > 0**.
    ①②는 **소진이 0 이어도** 행을 띄우기 위한 것이다 — 그렇지 않으면 사용자가 직접 만든
    조각이 주간 카드 어디에도 나타나지 않는다. ③은 **폐기·삭제로 흘러든 소진**을 잡는다
    (ADR-027 §3) — ①②만 보면 폐기 항목의 소진만 있는 주에서 행이 숨겨져 A24 가 깨진다.
    세 갈래가 모두 거짓인 주에는 행이 없다.
  - 그 행의 드릴다운에서 이름이 있는 것(소급 생성·직접 추가된 task)과 이름이 없는 것
    (미분류 세션)을 구분해 보여준다.
- **R18.** `dropped_at` 이 있는 항목은 일반 뷰 목록에 표시하지 않는다. 그 항목에 붙어
  있던 소진은 **R17 의 차액 정의에 따라 기타 행에 흡수되어 계속 보인다** — 폐기가
  주간 총 소진을 줄이지 않으므로 사용자는 3뽀모의 행방을 기타 행에서 추적할 수 있다.
  폐기는 삭제가 아니다: 이력에 남고 정산 요약에 집계된다 (ADR-014 §1).

### 예산과 과적

- **R19.** 주간 예산은 **해석된 개수를 `weeks.budget` 에 확정 저장한다** (ADR-013 §1).
  주를 계획하거나 정산할 때 그 시점에 해석된 값을 그대로 기록하므로, 과거 주의 예산
  표시는 정의상 불변이다. 다만 **컬럼 자체는 nullable** 이다 (ADR-018 §1) — 플래너를
  거치지 않고 첫 세션으로 `weeks` 행이 먼저 생긴 주는 `budget` 이 NULL 이며, 이는
  "예산 0"이 아니라 "기록 없음"이다.
  - 플래너의 예산 입력은 **기본값이 프리필된 상태로 시작**한다. 사용자가 손대지 않으면
    프리필 값이 그대로 저장된다 — "기본값이냐 오버라이드냐"는 데이터 모델에 없고
    입력 UI 의 관심사다.
  - **예산 결정 순서(무엇을 프리필로 쓰는지, `weeks` 행이 어느 시점에 어떤 스냅샷과
    함께 생기는지)는 이 문서가 재정의하지 않는다** — pomo-baseline 의 조회 계약과
    [ADR-013](../../architecture/decisions/adr-013-baseline-budget-effect.md) §1·§2 를
    따르고, 이 문서는 프리필된 값을 읽어 쓰는 쪽이다.
- **R20.** 주간 게이지는 `소진 / 예산` 을 표시한다. 소진은 그 주의 focus 세션 수
  전체(기타·미분류 포함)다.
  - `weeks` 행이 없는 주(계획도 정산도 세션도 없던 주)는 표시할 예산이 없고 소진도 0
    이므로, 바를 숨기고 "기록 없음"으로 표시한다 (ADR-013 §2).

  > ⚠️ 가정: capacity 가 미설정인 상태에서 `weeks` 행이 생기면 `budget` 에 무엇이
  > 저장되는지는 pomo-baseline 의 조회 계약 소관이다. 이 문서는 **`budget = 0` 을
  > "예산 미설정"과 동등하게 취급해 바를 숨기고 소진량만 표시**한다 — `소진 / 0` 을
  > 렌더하거나 0 으로 나누지 않는다.
- **R21.** 예산 **초과 시 빨간색·경고 아이콘·경고 문구를 쓰지 않는다** (원칙 3,
  principles §1·§2). 초과는 `--amber` 글로우 + `+N` 배지로 긍정 처리한다.
- **R22.** (계획 시점 검증 2종 — 둘 다 경고만, **확정 차단 금지**)
  1. **총량 과적:** 항목 est 합 > 예산이면 초과분을 알린다. 안내 문구는 **플래너 안에서
     실제로 가능한 조작만** 제시한다(est 낮추기 / 항목 덜어내기 / 예산 올리기) — 플래너에
     없는 액션("다음 주로 미루기")을 권하지 않는다.
  2. **요일별 부하:** 각 항목의 est 를 배정 요일 수로 균등 분산해 요일별 합을 내고,
     그 요일의 `weekly_capacity` 값과 비교한다. 미배치 항목과 기타 항목은 부하에
     산입하지 않는다.
- **R23.** 주중 재수정을 허용한다 (편집 대상 주를 `이번 주` 로 선택 — R3). 플래너 확정
  시 기존 항목은 **ID 기준으로 매칭**해 자식 task 이력과 `carry_from_id`·`origin_week`·
  `milestone_id` 를 유지한다 (제목 기준 매칭 금지 — 제목을 고치면 이력이 끊긴다).
  - 확정 시각은 `weeks.planned_at` 에 기록한다. **`planned_at` 은 그 주의 최초 확정
    시각만 담고, 주중 재수정으로 다시 확정해도 갱신하지 않는다** — 이 값이 답하는 질문은
    "이 주의 계획을 언제 세웠나"이고, 마지막으로 손댄 시각이 아니다. 재수정 이력을
    남기는 컬럼은 v1 에 두지 않는다.
- **R24.** 플래너 초안에서 행을 제거(`×`)할 때 결과는 **그 행이 기존 항목인지 신규
  초안인지에 따라 다르다.**
  - **기존 항목**(수정 진입 시 프리필된 행): 확정하면 **삭제가 아니라 `dropped_at`
    기록(폐기)** 이다. 자식 task 와 그에 붙은 세션은 **전부 보존**된다. 소진은 R18 에
    따라 기타 행에 흡수되어 계속 보인다. 세션 9개가 달린 항목을 × 해도 그 9뽀모는
    사라지지 않는다.
  - **신규 초안 행**(이 플래너 세션에서 추가했고 아직 확정되지 않은 행): 아무 데이터도
    만들지 않았으므로 제거는 **무해한 취소**다.
  - `deleted_at` 을 쓰는 진짜 삭제는 이 화면의 조작이 아니다 — 폐기와 삭제의 구분은
    [ADR-014](../../architecture/decisions/adr-014-deletion-and-archive.md) §1 을 따른다.

### 완료와 폐기

- **R25.** 소진 ≥ est 는 완료가 아니다. 항목 완료는 **자동 제안 + 수동 확정**이다
  (Q14) — 자식 task 가 전부 완료되면 완료를 제안하되, 완료의 사실을 만드는 것은 항상
  사용자 클릭이다. 자식 task 가 하나도 없는 항목도 수동으로 완료할 수 있다.
- **R26.** 완료는 `completed_at`, 폐기는 `dropped_at` 에 시각으로 기록한다. 둘 다 NULL
  이면 active 다 (ADR-011 §5). boolean 상태값을 쓰지 않는다.
- **R27.** 완료된 항목의 드로어는 열람 가능하되 **pull 만 차단**한다. 완료를 해제하면
  (`completed_at` 을 NULL 로) 다시 pull 할 수 있다.
- **R28.** **완료 확정은 소진 집계를 멈추지 않는다.** 완료 후에 그 항목의 자식 task 에
  세션이 더 붙으면(오늘 목록에 이미 있던 조각으로 계속 집중한 경우 등) 그 세션도 R8 의
  술어에 따라 항목 소진과 주간 총 소진에 반영된다.
  - 소진이 est 를 넘으면 완료 항목에도 초과 표시(`+N`)를 한다 — 사실이므로 숨기지 않는다.
  - **완료 제안은 재발생시키지 않는다.** 이미 `completed_at` 이 있는 항목에 완료를 다시
    제안하지 않으며, 세션이 추가돼도 `completed_at` 을 갱신하거나 NULL 로 되돌리지 않는다.

## 성공 지표

- **닫히는 게이지:** 임의의 주에 대해

  ```
  주간 게이지 소진 == Σ(is_system = 0 인 항목의 소진) + 기타 행 소진
  ```

  이 성립한다. 기타 행 소진이 이 등식의 차액으로 **정의**되므로(R17), 이 등식은 검증
  대상이 아니라 정의다. 실제 검증 대상은 R8 의 주 조건이 모든 항목 소진 계산 경로에
  들어 있는지(= 도메인 함수 한 곳만 쓰는지)이다.
- 계획 데이터가 0인 주에도 타이머·세션 기록이 정상 동작하고, 주간 카드는 빈 상태
  카피만 보여준다 (원칙 1).
- 과적 경고를 무시한 확정이 100% 성공한다 (R22 — 차단 0건).
- 주간 계획 소요 클릭 수는 지표로 삼지 않되, 예산 프리필 값을 그대로 쓰는 경로에서
  예산 관련 입력이 0회여야 한다 (§3.3 초안의 "기본값 유지 시 클릭 0" 승계).
- 일요일에 주간 카드의 `수정` 을 눌러 **이번 주 항목을 고칠 수 있다** (R3 — 계획일에도
  주중 재수정이 무효가 되지 않는다).

## 인수 기준

| # | 기준 | 요구사항 |
|---|---|---|
| A1 | 요일 핍·요일 칩·부하 그래프가 모두 월요일부터 시작하고, 주 키가 그 주 월요일 날짜다 | R1 |
| A2 | 주 번호 라벨을 저장하는 컬럼이 없고, 53주 연도(2026) 말에도 라벨이 어긋나지 않는다 | R2 |
| A3 | 일요일에 플래너를 열면 `다음 주` 가 기본 선택이고, 선택을 `이번 주` 로 바꾸면 이번 주 항목이 초안에 채워져 est 를 고쳐 확정할 수 있다 | R3, R23 |
| A4 | 일반 뷰는 편집 대상 주 선택과 무관하게 항상 오늘이 속한 주를 보여준다 | R4 |
| A5 | 편집 대상 주가 `이번 주` 면 헤더·CTA·확정 버튼이 모두 "이번 주", `다음 주` 면 모두 "다음 주" 로 일관되게 바뀐다 (요일에 따라 라벨과 실제 대상이 어긋나지 않는다) | R5 |
| A6 | 사용자가 만드는 항목의 est 스테퍼 하한이 1 이고, `is_system = 1` 항목만 est 0 으로 존재한다 | R6, R16 |
| A7 | 마일스톤을 연결하지 않은 항목이 경고 없이 저장되고, 연결된 마일스톤을 삭제하면 그 항목이 미연결로 남는다 | R6 |
| A8 | 요일이 배정된 항목이 그 요일에 오늘 목록으로 자동 유입되지 않는다 (상단 정렬·강조만) | R7 |
| A9 | 항목 소진을 저장하는 컬럼이 없고, 세션 1회 완료가 항목 행 숫자에 즉시 반영된다 | R8 |
| A10 | 일요일 23:50 에 시작해 자정을 넘긴 focus 세션이, 부모 항목이 지난 주 소속이면 지난 주 항목 소진에 세어지지 않고 **다음 주 기타 행**에 나타난다 | R8, R17 |
| A11 | 항목 est 3 · 자식 task est 합 5 인 상태가 경고·오류 없이 유지된다 | R9 |
| A12 | est 3 · 소진 5 인 항목의 "남은 몫"이 −2 가 아니라 0 이다 | R9 |
| A13 | 항목 목록이 생성순으로 정렬되고(오늘 배정만 상단), 순서를 저장하는 컬럼이 없다 | R10 |
| A14 | 2주를 건너뛰고 이월된 항목의 배지가 사슬 길이가 아닌 주차 차이로 계산돼 표시된다 | R11 |
| A15 | 3주 연속 이월된 항목의 배지가 `3주째` 다 (이월 항목이 `origin_week` 를 승계하므로 매주 1 로 리셋되지 않는다) | R11 |
| A16 | 자식 task 가 0개인 항목의 드로어에서 새 task 를 적어 `오늘로 가져오기` 하면 그 조각이 생성되고 동시에 오늘 목록에 올라간다 | R12 |
| A17 | 다음 주 항목의 드로어에서도 `오늘로 가져오기` 가 활성이고, 실행하면 `pull_date` 가 오늘로 기록된다 | R13 |
| A18 | 오늘 목록에서 task 를 뺀 뒤에도 그날 날짜의 pull 이력이 남는다 | R14 |
| A19 | 오늘 목록에 직접 추가한 task 가 기타 항목에 붙고, 기타 항목이 없던 주에는 그때 생성된다 | R15 |
| A20 | 자정을 걸친 세션에 사후 캡처로 조각을 붙이면, 조각이 **세션의 `local_week`** 주의 기타 항목에 붙는다 (오늘이 속한 주가 아니다) | R15 |
| A21 | 기타 항목의 est 0 이 과적 합계와 요일 부하 어디에도 더해지지 않고, 플래너 초안 목록에 나타나지 않는다 | R16 |
| A22 | 소급 생성 task 1건 + 미분류 세션 2건이 있으면 주간 카드에 행 1개로 표시되고, 드릴다운에서 둘이 구분된다 | R17 |
| A23 | 세션이 0인 직접 생성 조각만 있는 주에도 기타 행이 표시된다 (소진 0 이어도 숨기지 않는다) | R17 |
| A24 | 3뽀모를 소진한 항목을 폐기하면 항목은 목록에서 사라지지만 주간 총 소진은 줄지 않고, 그 3뽀모가 기타 행 값에 나타난다 | R17, R18 |
| A25 | 플래너를 열면 예산 입력에 값이 프리필돼 있고, 손대지 않고 확정하면 그 값이 `weeks.budget` 에 저장된다 (NULL 로 저장되는 경로가 없다) | R19 |
| A26 | 예산을 확정한 주의 게이지 값이, 그 뒤 capacity 를 바꿔도 변하지 않는다 | R19 |
| A27 | `weeks` 행이 없는 주와 `budget = 0` 인 주에서 게이지 바가 숨겨지고 소진량만 표시된다 (`소진 / 0` 을 렌더하지 않는다) | R20 |
| A28 | 예산 20 · 소진 23 인 주의 게이지에 빨간색·경고 문구가 없고 `+3` 배지가 표시된다 | R21 |
| A29 | est 합이 예산을 넘긴 상태에서도 확정 버튼이 동작하고, 안내 문구가 플래너 안에서 실제로 가능한 조작만 제시한다 | R22 |
| A30 | 플래너에서 기존 항목의 제목을 바꿔 확정해도 자식 task·이월 배지·마일스톤 연결이 유지된다 | R23 |
| A31 | 주중 재수정으로 다시 확정해도 `weeks.planned_at` 이 최초 확정 시각을 유지한다 | R23 |
| A32 | 세션 9개가 달린 기존 항목을 초안에서 × 하고 확정하면, 항목이 폐기 처리되고 자식 task·세션 9건이 모두 남아 있다 | R24 |
| A33 | 이 플래너 세션에서 방금 추가한 신규 초안 행을 × 하면 어떤 행도 만들어지지 않는다 | R24 |
| A34 | 자식 task 전부 완료 시 완료 제안이 뜨고, 클릭 전에는 항목이 active 로 남는다 | R25 |
| A35 | 완료·폐기 항목의 상태가 `completed_at`/`dropped_at` 시각으로 확인된다 | R26 |
| A36 | 완료된 항목의 드로어가 열리고 task 목록은 보이되 pull 은 불가하며, 완료 해제 시 다시 가능해진다 | R27 |
| A37 | est 3 을 완료 확정한 뒤 세션 2개가 더 붙으면 소진이 5 로 오르고 `+2` 가 표시되며, 완료 제안이 다시 뜨지 않고 `completed_at` 도 변하지 않는다 | R28 |
