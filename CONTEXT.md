# dongmodoro

1인용 로컬 뽀모도로 + 월/주/일 계획 Electron 앱의 도메인 용어집.
문서·코드 식별자 주석·UI 카피 전부 이 표기를 따른다. 여기 없는 용어를 새로
정하거나 기존 정의를 바꾸면, 그 결정을 만든 PR 에서 이 파일을 함께 갱신한다.

## Language

**뽀모**:
집계 화폐 단위. 완료된 focus 세션 1회 = 1뽀모. 휴식 세션은 뽀모로 집계하지 않는다.
_Avoid_: 뽀모도로, 포모

**세션**:
타이머 실행 1회가 남기는 기록 단위 (focus / short break / long break).
뽀모(화폐)와 별개 개념이다 — 휴식도 세션이지만 뽀모는 아니다.

**정산**:
지난 주(들)를 마감하는 행위와 그 화면. 정산 필요 판정 → 요약 → 남은 항목 이월
3택 → 확정으로 구성된다.
_Avoid_: 리뷰, 주간 리뷰, 회고 — 단 "코드 리뷰" 등 개발 프로세스 용법은 도메인
용어가 아니므로 무관하다.

**할당**:
한 주에 하겠다고 잡은 일 하나 (DB 의 `week_item` 엔티티). 예상 뽀모를 매기고 요일에
배치하며, 조각으로 쪼개 실행한다. 마일스톤에 선택적으로 연결된다.
코드·문서 산문에서 엔티티를 지칭하는 `week_item` 식별자 표기는 유지한다.
_Avoid_ (카피에서): 주간 항목, 아이템, 위크아이템

**조각**:
할당을 쪼갠 실행 단위 (DB 의 `task` 엔티티). 드로어에서 만들고, 오늘 목록으로
pull 하며, pull 된 뒤 화면에서는 "할 일"로 부른다.
코드·문서 산문에서 엔티티를 지칭하는 `task` 식별자 표기는 유지하되,
**렌더되는 카피에는 조각(또는 문맥상 할 일)만 쓴다.**
_Avoid_ (카피에서): task, 태스크, 서브태스크

**할 일**:
오늘 목록의 항목. 주간 조각을 pull 해 오거나 직접 추가한다.
_Avoid_: 작업, 태스크, 투두 — "작업표시줄" 같은 OS 용어는 무관하다.

**예상 뽀모**:
할당·조각에 계획 시점에 매기는 뽀모 수 (DB 의 `est_pomos`).
카피에서는 "예상 뽀모"로만 부른다.
_Avoid_ (카피에서): est, 견적, 추정 뽀모

**마일스톤**:
한 달의 결과물 (DB 의 `milestone` 엔티티). 제목(상태 문장) 하나만 갖고 **수치를 갖지
않는다** — 숫자가 처음 등장하는 레벨은 주다. 할당이 여기에 선택적으로 연결된다.
_Avoid_: 목표, 월간 목표, 월 계획

**보관**:
마일스톤을 그 달 카드의 목록 표시에서만 제외하는 것 (`archived_at`). 삭제가 아니며
**집계에 중립**이다 — 보관해도 달성 배지의 분모가 변하지 않는다.
_Avoid_: 아카이브, 숨김, 감추기

**롤업**:
마일스톤에 연결된 할당들의 소진을 **범위 라벨과 함께** 표시한 값 (예: `이번 주 3/8`).
지난달 카드의 `N/M 달성` 배지와는 별개 값이다.
_Avoid_: 진척률, 달성률(배지 전용어), 진행률

**공부한 날 수**:
이번 주에 완료 focus 세션이 1회 이상 있던 날의 수. **연속 일수가 아니다** — 사이가
비어도 각각 센다.
_Avoid_: 스트릭, 연속 기록, 연속 일수

## 표기 규칙

- 영문 슬러그(폴더명 `weekly-review`, 브랜치명 등)는 식별자이므로 유지한다.
  한국어 산문에서만 캐노니컬 용어를 강제한다.
- `docs/origin/` 과 `docs/decision-log/` 는 과거 기록이므로 용어를 소급 수정하지
  않는다.
