# dongmodoro

1인용 로컬 뽀모도로 + 월/주/일 계획 Electron 앱의 도메인 용어집.
문서·코드 식별자 주석·UI 카피 전부 이 표기를 따른다. 여기 없는 용어를 새로
정하거나 기존 정의를 바꾸면, 그 결정을 만든 PR 에서 이 파일을 함께 갱신한다.

## Language

**뽀모**:
타이머 사이클 단위. 완료된 focus 세션 1회 = 1뽀모이며 길이와 무관하다. 휴식 세션은
뽀모가 아니다. **집계·계획의 통화가 아니다** — 그 자리는 측정 시간이 갖는다
([ADR-030](docs/architecture/decisions/adr-030-time-as-progress-currency.md)).
_Avoid_: 뽀모도로, 포모

**세션**:
타이머 실행 1회가 남기는 기록 단위 (focus / short break / long break).
뽀모(사이클)와 별개 개념이다 — 휴식도 세션이지만 뽀모는 아니다.

**측정 시간**:
완료된 focus 세션에서 **타이머가 실제로 돈 시간**(`duration_sec`)을 조회 시점에
합산해 파생하는 집중 시간 표시. 어디에도 저장하지 않는다. 벽시계 경과가 아니다 —
일시정지 구간은 빠지고, 휴식 세션과 중단·폐기된 세션은 산입하지 않는다. 진행 중인
세션도 완료 전까지는 산입하지 않는다
([ADR-031](docs/architecture/decisions/adr-031-settlement-without-est.md) §3).
_Avoid_: 소진(뽀모 개수 시절의 용어), 소요 시간, 실적, 공부량, 실제 경과

**정산**:
지난 주(들)를 마감하는 행위와 그 화면. 정산 필요 판정 → 요약 → 남은 항목 처분
2택(`다음 주로` / `보내주기`) → 확정으로 구성된다
([ADR-031](docs/architecture/decisions/adr-031-settlement-without-est.md) §1).
_Avoid_: 리뷰, 주간 리뷰, 회고 — 단 "코드 리뷰" 등 개발 프로세스 용법은 도메인
용어가 아니므로 무관하다.

**Sprint**:
한 주에 하겠다고 잡은 일 하나 (DB 의 `week_item` 엔티티). 요일에 배치하며, task 로
쪼개 실행한다. Milestone 에 선택적으로 연결된다. 계획 시점에 매기는 숫자는 없다.
카드 제목(주 계획 전체)과 낱개 항목 둘 다 Sprint 로 부른다 (2026-08-17 결정).
코드·문서 산문에서 엔티티를 지칭하는 `week_item` 식별자 표기는 유지한다.
_Avoid_ (카피에서): 할당, 이번 주 할당, 주간 항목, 아이템, 위크아이템

**task**:
Sprint 를 쪼갠 실행 단위 (DB 의 `task` 엔티티). 드로어에서 만들고, 오늘 목록으로
pull 하며, pull 된 뒤 화면에서는 "할 일"로 부른다 (2026-08-17 결정 — 구 `조각`).
_Avoid_ (카피에서): 조각, 태스크(한글 표기), 서브태스크

**할 일**:
오늘 목록의 항목. 주간 task 를 pull 해 오거나 직접 추가한다.
_Avoid_: 작업, 태스크, 투두 — "작업표시줄" 같은 OS 용어는 무관하다.
task(주간 실행 단위)와는 층위가 다르다 — 오늘 목록에 들어온 뒤에만 할 일이다.

**Milestone**:
한 달의 결과물 (DB 의 `milestone` 엔티티). 제목(상태 문장) 하나만 갖고 **수치를 갖지
않는다** — 숫자가 처음 등장하는 레벨은 주다. Sprint 가 여기에 선택적으로 연결된다.
**날짜 제한 없이 어느 미래 달이든 계획할 수 있다** (2026-08-17 결정 — 구 "다음 달
한 칸" 선행 편집 제한 폐기). 지난 달은 읽기 전용이다.
_Avoid_ (카피에서): 결과물, 마일스톤(한글 표기), 목표, 월간 목표, 월 계획

**보관**:
Milestone 을 그 달 카드의 목록 표시에서만 제외하는 것 (`archived_at`). 삭제가 아니며
**집계에 중립**이다 — 보관해도 달성 배지의 분모가 변하지 않는다.
_Avoid_: 아카이브, 숨김, 감추기

**롤업**:
Milestone 에 연결된 Sprint 들의 측정 시간을 **범위 라벨과 함께** 표시한 값
(예: `이번 주 3시간 20분`). 지난달 카드의 `N/M 달성` 배지와는 별개 값이다.
_Avoid_: 진척률, 달성률(배지 전용어), 진행률

**공부한 날 수**:
이번 주에 완료 focus 세션이 1회 이상 있던 날의 수. **연속 일수가 아니다** — 사이가
비어도 각각 센다.
_Avoid_: 스트릭, 연속 기록, 연속 일수

## 폐기된 용어

뽀모 개수를 계획·집계의 통화로 쓰던 시절의 용어다. **새 문서·코드·카피에 쓰지 않는다.**
여기 남겨 두는 이유는, 기존 문서에서 이 단어를 만난 사람이 "아직 안 옮긴 것"인지
"죽은 것"인지 구분할 수 있게 하기 위해서다.

| 폐기 용어 | 무엇이었나 | 대신 |
|---|---|---|
| 예상 뽀모 (`est_pomos`) | 할당·조각에 계획 시점으로 매기던 개수 | 없음 — 계획 시점에 매기는 숫자 자체가 사라졌다 |
| 예산 (`weeks.budget`) | 한 주에 쓰겠다고 잡은 뽀모 총량 | 없음 — 측정 시간은 분모 없이 표시된다 |
| 가용량 (`weekly_capacity`) | 요일별로 낼 수 있다고 본 뽀모 수 | 없음 |
| 소진 | 지금까지 쓴 뽀모 개수 | 측정 시간 |
| 계획 대비 | 정산에서 예산과 소진을 비교하던 축 | 없음 — 정산은 사실만 요약한다 |
| 과적 | 예산을 넘긴 계획 상태 | 없음 (대체 신호는 미정) |
| 유효 베이스라인 · 유효 예산 | 주별로 박제된 길이·예산을 읽던 조회 | 길이는 전역 값 직독 |
| 임시 조절 | 세션 한 번만 적용되던 ± 조절 | 조절이 곧 기준이다 (ADR-033) |

근거: [ADR-030](docs/architecture/decisions/adr-030-time-as-progress-currency.md) ·
[ADR-031](docs/architecture/decisions/adr-031-settlement-without-est.md) ·
[ADR-033](docs/architecture/decisions/adr-033-adjust-as-baseline.md)

## 표기 규칙

- 영문 슬러그(폴더명 `weekly-review`, 브랜치명 등)는 식별자이므로 유지한다.
  한국어 산문에서만 캐노니컬 용어를 강제한다.
- `docs/origin/` 과 `docs/decision-log/` 는 과거 기록이므로 용어를 소급 수정하지
  않는다.
- 표기는 **릴리스**로 통일한다 (`릴리즈` ✕). ADR 본문과 `docs/plans/` 의 과거 표기는
  소급 수정하지 않는다.
