# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

Electron 데스크톱 앱 (렌더러는 웹 기술). 스택은 코드보다 먼저 ADR 로 확정돼 있고, 그
문서가 유일한 출처다 — [docs/architecture/overview.md](docs/architecture/overview.md).

- Electron + electron-vite + React 19 + TypeScript strict, 패키지 매니저는 pnpm
- better-sqlite3 + drizzle-orm (마이그레이션은 앱 시작 시 적용), zod, TanStack Query
- Tailwind CSS v4 + shadcn/ui, 아이콘 lucide-react
- Vitest + Testing Library. Playwright 는 v1 비범위
- main = 3층 백엔드(핸들러 → 서비스 → 순수 함수), renderer = FSD-lite, 경계는 zod 검증 IPC 만
- 구현 현황: **앱 코드 0줄.** 기획·설계 문서와 M1 스캐폴딩 계획까지만 존재한다
  ([docs/plans/2026-08-04-m1-scaffolding.md](docs/plans/2026-08-04-m1-scaffolding.md))

## Users

앱 소유자 1인. 로그인·멀티유저·동기화가 없는 로컬 단독 앱이다.

사용 상황은 두 가지로 갈린다.

- **계획하는 순간** — 주로 일요일 또는 주 시작 전. 이번 달 결과물을 정하고, 그것을 이번 주
  뽀모 몫으로 쪼개고, 요일에 배치한다. 주중 재수정도 정상 경로다.
- **실행하는 순간** — 하루 중 아무 때나. 오늘 목록에서 할 일을 골라 타이머를 돌린다.
  이때 계획 화면을 다시 열어 고민하지 않는 것이 목표다.

> 배포 범위 (2026-08-05 확인): v1 은 소유자 본인이 쓴다. 다만 **나중에 배포할 가능성을
> 닫지 않는다** — 백업·익스포트·콜드 스타트 품질을 지금부터 버리지 않는다. 온보딩·i18n·
> 에러 추적·자동 업데이트는 v1 범위가 아니지만, 그 부재를 되돌릴 수 없는 방식으로
> 설계하지 않는다.

## Product Purpose

월(결과물) → 주(얼마만큼) → 오늘(지금 뭐부터) 3층 계획과 뽀모도로 타이머를 **한 화면에서
끊기지 않게** 잇는다.

> 성공 기준 (2026-08-05 확인): **계획과 실행이 한 화면에서 이어지는 것 자체가 성공이다.**
> 노션·투두 앱과 타이머를 왕복하지 않게 되는 것. 소진량·달성률·연속 일수는 성공 지표로
> 삼지 않는다.

각 층이 답하는 질문이 다르고 섞이지 않는다.

| 층 | 답하는 질문 | 숫자 |
|---|---|---|
| 월 마일스톤 | 이번 달이 끝나면 뭐가 달라져 있나 | **없다** — 수치 입력 필드를 두지 않는다 |
| 주간 할당 | 얼마만큼 | 뽀모 개수(est·예산)가 처음 등장하는 층 |
| 오늘 목록 | 지금 뭐부터 | 없다 — 주간 풀에서 pull 로만 채운다 |

## Positioning

이웃 제품이 그대로 베낄 수 없는 메커니즘 4개.

1. **뽀모 개수가 단일 통화다.** 1뽀모 = focus 세션 1회 완료이며 **길이와 무관하다.**
   12분에 완료 처리한 세션도, 35분까지 늘린 세션도 각각 1뽀모다. 그래서 길이를 바꿔도
   과거 기록이 재해석되지 않는다.
2. **소진량을 저장하지 않는다.** 모든 진행 표시는 세션에서 파생되는 아래→위 단방향
   값이다. 월 카드의 롤업도 주간 항목 합에서 올라온다. 두 화면이 다른 숫자를 말하는
   상태가 구조적으로 불가능하다.
3. **편집은 언제든, 효력은 다음 주 경계부터.** 뽀모 길이·가용량을 수요일에 바꿔도 진행
   중인 주의 분모가 흔들리지 않는다. 편집 경로를 막는 대신 효력 시점으로 닫았다.
4. **막는 화면이 없다.** 과적이어도 계획 확정은 항상 성공하고, 정산 패널·드로어·오버레이는
   모두 비모달이며, 계획 0개도 오류가 아닌 정상 상태다. 클릭 1회로 확정해도 합리적
   결과가 나온다.

## Operating Context

- **완전 로컬·오프라인.** DB 는 사용자 데이터 폴더의 SQLite 파일 하나. 서버·계정·네트워크
  의존이 없다. 폰트도 로컬 번들이다.
- **대상 OS (2026-08-05 확인): macOS · Windows · Linux 셋 다.** 타이틀바 슬롯 순서와
  종료 단축키가 플랫폼 조건으로 갈리고(macOS 좌측 트래픽 라이트 / Windows·Linux 우측
  컨트롤 폭 예약), 트레이 동작은 Linux 데스크톱 환경마다 검증이 필요하다.
- **창을 닫아도 앱은 살아 있다.** 창 닫기(×·⌘W·Alt+F4)는 종료가 아니라 표시를 끄는
  것이고, 타이머·완료 판정·OS 알림은 main 이 계속 수행한다. 종료는 별개 개념(⌘Q·앱 메뉴
  Quit·트레이 종료·OS 주도)이며, focus 진행 중이면 확인을 1회 거친다.
- **창은 항상 0개 또는 1개.** 복귀 경로 4개(트레이·OS 알림·Dock/작업표시줄·재실행 시도)가
  모두 같은 동작으로 수렴한다. 단일 인스턴스 잠금이 없으면 두 프로세스가 같은 DB 를 열어
  타이머가 2개가 된다.
- **트레이는 창 닫기=숨김과 한 배포 단위다.** 트레이가 없는 빌드에서는 창 닫기가 종료로
  동작한다 — 복귀 경로 0개인 유령 상태를 어떤 중간 빌드에서도 만들지 않는다.
- **주는 월요일 시작**이고 주 키는 그 주 월요일 날짜다. 하루 경계는 **로컬 자정 고정**
  (오프셋 설정 없음), 세션의 날짜 귀속은 시작 시각 기준이다. 달력 키는 쓰는 순간 1회
  계산해 불변 저장하므로 타임존을 바꿔도 과거 기록이 이동하지 않는다.

## Capabilities and Constraints

기능 8개의 확정 명세는 [docs/features/](docs/features/README.md) 가 소유한다:
app-shell · timer · today-tasks · week-plan · weekly-review · milestones ·
calendar-records · pomo-baseline.

**구조적 불변식** (기능이 아니라 데이터 모델이 보장한다)

- 부모 없는 task 는 존재하지 않는다. 오늘 목록 직접 입력도 시스템 "기타" 항목에 배선된다.
- 세션 귀속은 `세션의 주 = 항목의 주` 조건으로 닫히므로, 부모 항목이 완료·폐기·이월돼도
  그 세션은 자기 주에서 정확히 한 번 세어진다.
- 세션이 하나라도 있는 주는 반드시 자기 분모 스냅샷(길이·가용량·예산)을 갖는다.
- 삭제는 대부분 soft 이거나 보관이다. 물리 삭제는 마일스톤 하나뿐이고 확인을 요구한다.
- 분모를 읽는 경로는 조회 계약 2개뿐이다 — 다른 화면이 "없으면 전역값" 류 폴백을
  복제하지 않는다.

**v1 비범위** (지어내지 말 것)

월 누적 소진 집계, 미래 날짜 pull, 연속 일수(스트릭) 정의, 기록 내보내기·연간 히트맵,
상시 설정 화면(편집 경로는 첫 실행 온보딩과 정산 진입점 2개뿐), task 단위 요일 배정,
크래시 복원, 동기화 엔진, 월말 정산의 마일스톤 재설정 흐름, 시점별 완료 여부 복원.

**미결정** (결정 시점이 정해진 것)

- Query invalidation 키 계층 — 타이머 착수 직전 ADR
- 최소 창 크기 확정값 (목표 ~420×640) — 셸 구현 시 실측
- 단축키 표, 스크린리더 라이브 영역 문구
- macOS 코드 서명·공증, 트레이 아이콘 형태·남은 시간 표시 — 패키징 단계(M4)

## Brand Commitments

- 제품명 **dongmodoro**.
- **용어는 [CONTEXT.md](CONTEXT.md) 용어집이 유일한 출처다.** 캐노니컬 용어만 쓴다
  (정산 ○ / 리뷰 ✕, 뽀모 ○ / 뽀모도로 ✕, 할 일 ○ / 작업·태스크 ✕). 용어를 새로 정하는
  결정은 같은 PR 에서 용어집을 갱신한다.
- **렌더되는 UI 에 이모지를 쓰지 않는다.** 아이콘은 lucide-react 컴포넌트, lucide 에 없는
  도메인 심볼(뽀모 도트 등)은 토큰 기반 커스텀 SVG/CSS 로 만든다. 초안 문서의 🍅 는
  문서용 속기일 뿐 구현 지시가 아니다.
- 폰트는 **로컬 번들 2종** — 본문 Pretendard, 숫자·타이머 Azeret Mono(tabular-nums 필수).
  오프라인 동작이 요구사항이라 웹폰트 CDN 을 쓰지 않는다.
- **문구 톤은 사실만 말하고 판단하지 않는다.** 금지 표현이 명시돼 있다: "미달성·실패·
  밀린·숙제·지연·N건 남음" 류 결핍 프레임, "버리세요·정리하세요" 류 훈계, "잘했어요" 류
  칭찬·평가. 예산 초과는 실패가 아니라 사실로 표시한다.
- 시각 토큰의 유일한 출처는 [docs/design-system/tokens.md](docs/design-system/tokens.md),
  시각 판단은 [principles.md](docs/design-system/principles.md) 이며 충돌 시 후자가 이긴다.
  값 추가·변경은 ADR 선행이다.

## Evidence on Hand

**있는 것**

- `docs/origin/` — 초안 PRD + 시안 v7 HTML. **읽기 전용이고 확정 명세가 아니다.** 도구
  레벨(hook + permissions.deny)로 수정이 차단돼 있다. 설계 결정이 origin 과 어긋나도 되며
  충돌 시 features·architecture 문서가 이긴다.
- `docs/features/` — 기능 8개 문서 세트(overview·prd, 필요한 곳만 ux-spec·technical-spec).
  Harness 체크리스트 8/8 통과, 깨진 링크 0.
- `docs/architecture/decisions/` — ADR 15건. `docs/design-system/decisions/` — ADR 2건
  (브레이크포인트, 유리 표면 토큰).
- `docs/decision-log/` — 결정 과정 기록. 기각된 선택지와 사용자 원문 이유를 포함한다.
- `docs/design-system/wireframes/v1-wireframe.html` — 뷰 8개 와이어프레임. 토큰만 사용.
  **명세가 아니라 구속력 없는 시각 참조**이며 ux-spec 과 충돌하면 ux-spec 이 이긴다.

**없는 것 — 지어내지 말 것**

실사용자·인터뷰·설문, 사용 지표·벤치마크, 후기·사례·인용, 가격·라이선스·배포 채널,
실행 스크린샷(코드가 0줄이라 존재할 수 없다), 로고·아이콘 에셋, 실제 사용 데이터.

## Product Principles

1. **계획이 0개여도 앱은 완전히 동작한다.** 타이머는 첫 프레임부터 돌아가고, 빈 상태는
   오류가 아니라 "아직 안 정했음"이다. 환영 모달·단계별 투어를 만들지 않는다.
2. **층마다 답하는 질문이 하나다.** 월은 결과물, 주는 얼마만큼, 오늘은 순서. 같은 질문에
   두 번 답하게 만들면 어느 쪽이 진짜 계획인지 알 수 없어진다.
3. **초과는 실패가 아니다.** 예산은 추정치이고, 넘어간 것은 앰버·불꽃으로 긍정 처리한다.
   경고색·차단·부정 표현을 만들지 않는다.
4. **사실만 표시하고 판단은 사용자에게.** 이월 3주째든 5주째든 같은 색·같은 문구다.
   시스템이 "버릴 때예요"라고 말하지 않는다.
5. **아무것도 건드리지 않고 확정해도 합리적 결과가 나온다.** 기본 선택이 비어 있는 행을
   만들지 않고, 프리필된 값 그대로 확정하는 경로가 항상 유효하다.
6. **파생은 아래에서 위로만.** 집계값을 저장하지 않는다. 두 화면이 같은 사실에 대해 다른
   숫자를 말할 수 없어야 한다.

## Accessibility & Inclusion

제품 고유 요구가 확립된 것은 아래 4개다. 그 밖의 상세(단축키 표·스크린리더 문구)는 미결.

- `prefers-reduced-motion: reduce` 를 **항상 존중**한다. 배경 광원·별은 정적 렌더이고,
  장식성 무한 애니메이션(초과 펄스 포함)을 만들지 않는다.
- 내로우 탭은 `tablist`/`tab`/`tabpanel` role 과 좌우 화살표 이동을 갖는다.
- MONTH 오버레이는 **비모달이므로 포커스 트랩을 걸지 않는다** — Tab 으로 오버레이 밖
  (타이머·주간·오늘)에 도달할 수 있어야 한다.
- 색만으로 의미를 전달하지 않는다. 파괴적 색(`--danger`)을 쓰는 요소는 아이콘·문구가
  의미를 함께 전달한다. 구간 전환으로 포커스된 요소가 사라지면 포커스는 그 요소를 담고
  있던 카드 컨테이너로 옮기고, 문서 처음으로 되돌리지 않는다.
